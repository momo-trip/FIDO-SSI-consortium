export type DIDtype = {
    id: string;
    did: string;
    pk: string;
    sk: string;
}


export type VCtype = {
    id: string;
    title: string;
    issuer: string;
    did: string;
    pk: string;
}